@extends('layouts.app')

@section('title', 'Reports Management - E-Benta Admin')

@section('content')
<style>
    /* === REPORTS WRAPPER === */
    .reports-management-wrapper {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        min-height: 100vh;
        padding: 2rem 0;
    }

    /* === HEADER SECTION === */
    .reports-mgmt-header {
        background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        color: white;
        padding: 2.5rem 2rem;
        margin-bottom: 2rem;
        position: relative;
        overflow: hidden;
    }

    .reports-mgmt-header::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -10%;
        width: 500px;
        height: 500px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        z-index: 0;
    }

    .reports-mgmt-header::after {
        content: '';
        position: absolute;
        bottom: -30%;
        left: -5%;
        width: 300px;
        height: 300px;
        background: rgba(255, 255, 255, 0.08);
        border-radius: 50%;
        z-index: 0;
    }

    .reports-mgmt-header-content {
        position: relative;
        z-index: 1;
        display: flex;
        align-items: center;
        gap: 1.5rem;
    }

    .reports-header-icon {
        background: rgba(255, 255, 255, 0.2);
        padding: 1rem 1.25rem;
        border-radius: 1rem;
        font-size: 2rem;
    }

    .reports-mgmt-header h1 {
        font-size: 2.2rem;
        font-weight: 900;
        margin: 0 0 0.5rem 0;
        letter-spacing: -0.5px;
    }

    .reports-mgmt-header p {
        opacity: 0.95;
        margin: 0;
        font-size: 0.95rem;
    }

    /* === STAT CARDS === */
    .stat-card-reports {
        background: white;
        border-radius: 1.2rem;
        padding: 1.75rem;
        border: 1px solid rgba(220, 38, 38, 0.08);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        transition: all 0.3s ease;
        margin-bottom: 1.5rem;
        border-top-width: 4px;
    }

    .stat-card-reports:hover {
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
        transform: translateY(-4px);
    }

    .stat-card-red {
        border-top-color: #ef4444;
    }

    .stat-card-orange {
        border-top-color: #f97316;
    }

    .stat-card-blue {
        border-top-color: #3b82f6;
    }

    .stat-card-green {
        border-top-color: #22c55e;
    }

    .stat-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }

    .stat-icon-box {
        width: 60px;
        height: 60px;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
    }

    .icon-red {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.15) 0%, rgba(239, 68, 68, 0.08) 100%);
        color: #ef4444;
    }

    .icon-orange {
        background: linear-gradient(135deg, rgba(249, 115, 22, 0.15) 0%, rgba(249, 115, 22, 0.08) 100%);
        color: #f97316;
    }

    .icon-blue {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(59, 130, 246, 0.08) 100%);
        color: #3b82f6;
    }

    .icon-green {
        background: linear-gradient(135deg, rgba(34, 197, 94, 0.15) 0%, rgba(34, 197, 94, 0.08) 100%);
        color: #22c55e;
    }

    .stat-label {
        color: #64748b;
        font-size: 0.8rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin: 0;
    }

    .stat-value {
        font-size: 2rem;
        font-weight: 900;
        margin: 0.75rem 0 0.5rem 0;
    }

    .stat-description {
        color: #64748b;
        font-size: 0.85rem;
        margin: 0;
        font-weight: 500;
    }

    /* === FILTER SECTION === */
    .filter-card-reports-mgmt {
        background: white;
        border-radius: 1.2rem;
        padding: 1.8rem;
        border: 1px solid rgba(220, 38, 38, 0.1);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        margin-bottom: 2rem;
    }

    .filter-title {
        color: #1e293b;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
    }

    .filter-title i {
        color: #dc2626;
    }

    .filter-select-reports-mgmt {
        background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
        color: #1e293b;
        border: 1px solid rgba(220, 38, 38, 0.15);
        padding: 0.85rem 1rem;
        border-radius: 0.8rem;
        font-weight: 500;
        transition: all 0.3s ease;
        font-size: 0.95rem;
    }

    .filter-select-reports-mgmt:focus {
        border-color: rgba(220, 38, 38, 0.4);
        box-shadow: 0 0 15px rgba(220, 38, 38, 0.15);
        background: white;
        outline: none;
    }

    .filter-input-reports-mgmt {
        background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
        color: #1e293b;
        border: 1px solid rgba(220, 38, 38, 0.15);
        padding: 0.85rem 1rem;
        border-radius: 0.8rem;
        font-weight: 500;
        transition: all 0.3s ease;
        font-size: 0.95rem;
    }

    .filter-input-reports-mgmt::placeholder {
        color: #cbd5e1;
    }

    .filter-input-reports-mgmt:focus {
        border-color: rgba(220, 38, 38, 0.4);
        box-shadow: 0 0 15px rgba(220, 38, 38, 0.15);
        background: white;
        outline: none;
    }

    .filter-btn-reports-mgmt {
        background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        color: white;
        border: none;
        padding: 0.85rem 1.5rem;
        font-weight: 700;
        border-radius: 0.8rem;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(220, 38, 38, 0.25);
        cursor: pointer;
        font-size: 0.95rem;
        display: flex;
        align-items: center;
        gap: 0.6rem;
        width: 100%;
    }

    .filter-btn-reports-mgmt:hover {
        box-shadow: 0 6px 20px rgba(220, 38, 38, 0.35);
        transform: translateY(-2px);
    }

    /* === TABLE SECTION === */
    .table-card-reports-mgmt {
        background: white;
        border-radius: 1.2rem;
        border: 1px solid rgba(220, 38, 38, 0.1);
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .table-header-reports-mgmt {
        background: linear-gradient(135deg, rgba(220, 38, 38, 0.08) 0%, rgba(220, 38, 38, 0.04) 100%);
        border-bottom: 1px solid rgba(220, 38, 38, 0.15);
        padding: 1.5rem;
    }

    .table-header-reports-mgmt h5 {
        margin: 0;
        color: #1e293b;
        font-weight: 800;
        font-size: 1.1rem;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .table-header-reports-mgmt i {
        color: #dc2626;
    }

    .reports-table {
        color: #1e293b;
        margin-bottom: 0;
        border-collapse: separate;
        border-spacing: 0;
    }

    .reports-table thead {
        background: linear-gradient(135deg, rgba(220, 38, 38, 0.06) 0%, rgba(220, 38, 38, 0.03) 100%);
        border-bottom: 2px solid rgba(220, 38, 38, 0.15);
    }

    .reports-table thead th {
        color: #dc2626;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 0.8rem;
        padding: 1.25rem 1rem;
    }

    .reports-table tbody tr {
        border-bottom: 1px solid rgba(220, 38, 38, 0.08);
        transition: background 0.2s ease;
    }

    .reports-table tbody tr:hover {
        background: rgba(220, 38, 38, 0.04);
    }

    .reports-table td {
        padding: 1.25rem 1rem;
        vertical-align: middle;
    }

    .report-id {
        color: #dc2626;
        font-weight: 700;
        background: rgba(220, 38, 38, 0.08);
        padding: 0.3rem 0.6rem;
        border-radius: 0.3rem;
        font-family: monospace;
    }

    .reporter-info strong {
        color: #1e293b;
        display: block;
        margin-bottom: 0.3rem;
    }

    .reporter-email {
        color: #64748b;
        font-size: 0.85rem;
    }

    .item-type {
        background: linear-gradient(135deg, rgba(3, 102, 214, 0.12) 0%, rgba(3, 102, 214, 0.06) 100%);
        color: #0366d6;
        padding: 0.4rem 0.75rem;
        border-radius: 0.4rem;
        font-weight: 600;
        font-size: 0.85rem;
        display: inline-block;
        border: 1px solid rgba(3, 102, 214, 0.2);
    }

    .reason-badge {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.15) 0%, rgba(239, 68, 68, 0.08) 100%);
        color: #ef4444;
        padding: 0.5rem 0.9rem;
        border-radius: 0.4rem;
        font-weight: 700;
        font-size: 0.8rem;
        display: inline-block;
        border: 1px solid rgba(239, 68, 68, 0.2);
    }

    .status-badge-reports {
        padding: 0.5rem 0.9rem;
        border-radius: 0.4rem;
        font-size: 0.8rem;
        font-weight: 700;
        display: inline-block;
        border: 1px solid;
    }

    .status-pending-reports {
        background: rgba(249, 115, 22, 0.15);
        color: #f97316;
        border-color: rgba(249, 115, 22, 0.2);
    }

    .status-review-reports {
        background: rgba(59, 130, 246, 0.15);
        color: #3b82f6;
        border-color: rgba(59, 130, 246, 0.2);
    }

    .status-resolved-reports {
        background: rgba(34, 197, 94, 0.15);
        color: #22c55e;
        border-color: rgba(34, 197, 94, 0.2);
    }

    .status-dismissed-reports {
        background: rgba(107, 114, 128, 0.15);
        color: #6b7280;
        border-color: rgba(107, 114, 128, 0.2);
    }

    .date-meta {
        color: #64748b;
        font-size: 0.85rem;
    }

    .action-btn {
        background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        color: white;
        border: none;
        font-weight: 700;
        padding: 0.5rem 1rem;
        border-radius: 0.6rem;
        font-size: 0.8rem;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        transition: all 0.3s ease;
        box-shadow: 0 2px 8px rgba(220, 38, 38, 0.2);
        cursor: pointer;
    }

    .action-btn:hover {
        box-shadow: 0 4px 12px rgba(220, 38, 38, 0.35);
        transform: translateY(-2px);
        color: white;
        text-decoration: none;
    }

    /* === PAGINATION === */
    .pagination-wrapper-reports {
        padding: 1.5rem;
        border-top: 1px solid rgba(220, 38, 38, 0.1);
        display: flex;
        justify-content: center;
    }

    /* === EMPTY STATE === */
    .empty-state-reports {
        padding: 3rem 2rem;
        text-align: center;
    }

    .empty-icon-reports {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        background: linear-gradient(135deg, rgba(220, 38, 38, 0.12) 0%, rgba(220, 38, 38, 0.06) 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 2.5rem;
        color: #dc2626;
    }

    .empty-title-reports {
        color: #1e293b;
        margin-bottom: 0.75rem;
        font-weight: 800;
        font-size: 1.2rem;
        letter-spacing: -0.5px;
    }

    .empty-message-reports {
        color: #64748b;
        margin: 0;
        font-weight: 500;
    }

    /* === DARK MODE === */
    body.dark-mode .reports-management-wrapper {
        background: linear-gradient(135deg, #1a1a1a 0%, #222222 100%);
    }

    body.dark-mode .filter-card-reports-mgmt,
    body.dark-mode .table-card-reports-mgmt,
    body.dark-mode .stat-card-reports {
        background: #2a2a2a;
        border-color: rgba(220, 38, 38, 0.2);
    }

    body.dark-mode .filter-title,
    body.dark-mode .table-header-reports-mgmt h5,
    body.dark-mode .stat-label,
    body.dark-mode .reports-table td {
        color: #e0e0e0;
    }

    body.dark-mode .filter-select-reports-mgmt,
    body.dark-mode .filter-input-reports-mgmt {
        background: #333333;
        border-color: rgba(220, 38, 38, 0.3);
        color: #e0e0e0;
    }

    body.dark-mode .reports-table thead {
        background: rgba(220, 38, 38, 0.1);
    }

    body.dark-mode .reports-table tbody tr:hover {
        background: rgba(220, 38, 38, 0.08);
    }

    /* === RESPONSIVE === */
    @media (max-width: 768px) {
        .reports-mgmt-header h1 {
            font-size: 1.8rem;
        }

        .stat-value {
            font-size: 1.5rem;
        }

        .reports-table {
            font-size: 0.85rem;
        }

        .reports-table td {
            padding: 0.85rem 0.5rem;
        }

        .filter-btn-reports-mgmt {
            width: 100%;
        }
    }
</style>

<!-- Include Sidebar -->
@include('admin.sidebar')

<div class="main-content-wrapper" style="margin-left: 260px; overflow-x: hidden; min-height: 100vh; transition: margin-left 0.2s ease, width 0.2s ease; width: calc(100% - 260px); box-sizing: border-box;">
    <div class="reports-management-wrapper">
        <!-- Header -->
        <div class="reports-mgmt-header">
            <div class="container-fluid">
                <div class="reports-mgmt-header-content">
                    <div class="reports-header-icon">
                        <i class="fas fa-flag"></i>
                    </div>
                    <div>
                        <h1>Reports Management</h1>
                        <p>Review and manage user reports for inappropriate content, scams, and violations</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="container-fluid" style="padding: 0 2rem;">
            <!-- Statistics Cards -->
            <div class="row">
                <div class="col-lg-3 col-md-6">
                    <div class="stat-card-reports stat-card-red">
                        <div class="stat-header">
                            <div class="stat-icon-box icon-red">
                                <i class="fas fa-flag"></i>
                            </div>
                            <div>
                                <p class="stat-label">Total Reports</p>
                            </div>
                        </div>
                        <h3 class="stat-value" style="color: #ef4444;">{{ $reports->total() }}</h3>
                        <p class="stat-description">All submitted reports</p>
                    </div>
                </div>

                <div class="col-lg-3 col-md-6">
                    <div class="stat-card-reports stat-card-orange">
                        <div class="stat-header">
                            <div class="stat-icon-box icon-orange">
                                <i class="fas fa-hourglass-half"></i>
                            </div>
                            <div>
                                <p class="stat-label">Pending</p>
                            </div>
                        </div>
                        <h3 class="stat-value" style="color: #f97316;">
                            @php
                                $pending = $reports->getCollection()->filter(fn($r) => $r->status === 'pending')->count();
                            @endphp
                            {{ $pending }}
                        </h3>
                        <p class="stat-description">Awaiting review</p>
                    </div>
                </div>

                <div class="col-lg-3 col-md-6">
                    <div class="stat-card-reports stat-card-blue">
                        <div class="stat-header">
                            <div class="stat-icon-box icon-blue">
                                <i class="fas fa-magnifying-glass"></i>
                            </div>
                            <div>
                                <p class="stat-label">Under Review</p>
                            </div>
                        </div>
                        <h3 class="stat-value" style="color: #3b82f6;">
                            @php
                                $review = $reports->getCollection()->filter(fn($r) => $r->status === 'under_review')->count();
                            @endphp
                            {{ $review }}
                        </h3>
                        <p class="stat-description">Currently being processed</p>
                    </div>
                </div>

                <div class="col-lg-3 col-md-6">
                    <div class="stat-card-reports stat-card-green">
                        <div class="stat-header">
                            <div class="stat-icon-box icon-green">
                                <i class="fas fa-check-circle"></i>
                            </div>
                            <div>
                                <p class="stat-label">Resolved</p>
                            </div>
                        </div>
                        <h3 class="stat-value" style="color: #22c55e;">
                            @php
                                $resolved = $reports->getCollection()->filter(fn($r) => $r->status === 'resolved')->count();
                            @endphp
                            {{ $resolved }}
                        </h3>
                        <p class="stat-description">Completed actions</p>
                    </div>
                </div>
            </div>

            <!-- Filters -->
            <div class="filter-card-reports-mgmt">
                <h6 class="filter-title">
                    <i class="fas fa-filter"></i>Filter Reports
                </h6>
                <form method="GET" action="{{ route('admin.reports.index') }}" class="row g-3">
                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-toggle-on me-1" style="color: #dc2626;"></i>Status
                        </label>
                        <select name="status" class="filter-select-reports-mgmt">
                            <option value="">All Statuses</option>
                            <option value="pending" {{ request('status') == 'pending' ? 'selected' : '' }}>Pending</option>
                            <option value="under_review" {{ request('status') == 'under_review' ? 'selected' : '' }}>Under Review</option>
                            <option value="resolved" {{ request('status') == 'resolved' ? 'selected' : '' }}>Resolved</option>
                            <option value="dismissed" {{ request('status') == 'dismissed' ? 'selected' : '' }}>Dismissed</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-exclamation-circle me-1" style="color: #dc2626;"></i>Reason
                        </label>
                        <select name="reason" class="filter-select-reports-mgmt">
                            <option value="">All Reasons</option>
                            <option value="scam_fraud" {{ request('reason') == 'scam_fraud' ? 'selected' : '' }}>Scam/Fraud</option>
                            <option value="inappropriate_content" {{ request('reason') == 'inappropriate_content' ? 'selected' : '' }}>Inappropriate Content</option>
                            <option value="harassment_abuse" {{ request('reason') == 'harassment_abuse' ? 'selected' : '' }}>Harassment/Abuse</option>
                            <option value="fake_listing" {{ request('reason') == 'fake_listing' ? 'selected' : '' }}>Fake Listing</option>
                            <option value="seller_unresponsive" {{ request('reason') == 'seller_unresponsive' ? 'selected' : '' }}>Seller Unresponsive</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-list me-1" style="color: #dc2626;"></i>Type
                        </label>
                        <select name="type" class="filter-select-reports-mgmt">
                            <option value="">All Types</option>
                            <option value="Listing" {{ request('type') == 'Listing' ? 'selected' : '' }}>Listings</option>
                            <option value="Offer" {{ request('type') == 'Offer' ? 'selected' : '' }}>Offers</option>
                            <option value="User" {{ request('type') == 'User' ? 'selected' : '' }}>Users</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-search me-1" style="color: #dc2626;"></i>Search
                        </label>
                        <input type="text" name="search" placeholder="Search report details..." value="{{ request('search') }}" class="filter-input-reports-mgmt" style="width: 100%;">
                    </div>

                    <div class="col-12">
                        <button type="submit" class="filter-btn-reports-mgmt">
                            <i class="fas fa-search"></i>Apply Filters
                        </button>
                    </div>
                </form>
            </div>

            <!-- Reports Table -->
            <div class="table-card-reports-mgmt">
                <div class="table-header-reports-mgmt">
                    <h5><i class="fas fa-list"></i>All Reports</h5>
                </div>

                <div class="table-responsive">
                    @if($reports->count() > 0)
                        <table class="reports-table w-100">
                            <thead>
                                <tr>
                                    <th><i class="fas fa-hashtag me-1"></i>ID</th>
                                    <th><i class="fas fa-user me-1"></i>Reporter</th>
                                    <th><i class="fas fa-link me-1"></i>Item</th>
                                    <th><i class="fas fa-exclamation me-1"></i>Reason</th>
                                    <th><i class="fas fa-info-circle me-1"></i>Status</th>
                                    <th><i class="fas fa-calendar me-1"></i>Submitted</th>
                                    <th><i class="fas fa-bolt me-1"></i>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach($reports as $report)
                                    <tr>
                                        <td>
                                            <span class="report-id">#{{ $report->id }}</span>
                                        </td>
                                        <td>
                                            <div class="reporter-info">
                                                <strong>{{ $report->reporter->name }}</strong>
                                                <span class="reporter-email">{{ $report->reporter->email }}</span>
                                            </div>
                                        </td>
                                        <td>
                                            @php
                                                $type = class_basename($report->reportable_type);
                                            @endphp
                                            <span class="item-type">
                                                {{ $type }}
                                                @if($type === 'Listing')
                                                    <span style="margin-left: 0.3rem;">({{ $report->reportable->category ?? 'N/A' }})</span>
                                                @elseif($type === 'Offer')
                                                    <span style="margin-left: 0.3rem;">#{{ $report->reportable->id }}</span>
                                                @endif
                                            </span>
                                        </td>
                                        <td>
                                            <span class="reason-badge">
                                                {{ str_replace('_', ' ', ucfirst($report->reason)) }}
                                            </span>
                                        </td>
                                        <td>
                                            @php
                                                $statusClass = match($report->status) {
                                                    'pending' => 'status-pending-reports',
                                                    'under_review' => 'status-review-reports',
                                                    'resolved' => 'status-resolved-reports',
                                                    'dismissed' => 'status-dismissed-reports',
                                                    default => 'status-pending-reports'
                                                };
                                                $statusText = match($report->status) {
                                                    'pending' => 'Pending',
                                                    'under_review' => 'Under Review',
                                                    'resolved' => 'Resolved',
                                                    'dismissed' => 'Dismissed',
                                                    default => 'Unknown'
                                                };
                                            @endphp
                                            <span class="status-badge-reports {{ $statusClass }}">
                                                {{ $statusText }}
                                            </span>
                                        </td>
                                        <td>
                                            <span class="date-meta">{{ $report->created_at->diffForHumans() }}</span>
                                        </td>
                                        <td>
                                            <a href="{{ route('admin.reports.show', $report) }}" class="action-btn">
                                                <i class="fas fa-eye"></i>Review
                                            </a>
                                        </td>
                                    </tr>
                                @endforeach
                            </tbody>
                        </table>
                        <div class="pagination-wrapper-reports">
                            {{ $reports->links() }}
                        </div>
                    @else
                        <div class="empty-state-reports">
                            <div class="empty-icon-reports">
                                <i class="fas fa-inbox"></i>
                            </div>
                            <h5 class="empty-title-reports">No reports found</h5>
                            <p class="empty-message-reports">There are no reports matching your filters. Your users are behaving well!</p>
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>

@endsection

        <div class="col-lg-3 col-md-6 mb-4">
            <div style="background: linear-gradient(135deg, #ffffff 0%, #f0f7ff 100%); border: 1px solid rgba(249, 115, 22, 0.15); border-top: 4px solid #f97316; border-radius: 1.2rem; padding: 1.5rem; box-shadow: 0 4px 15px rgba(249, 115, 22, 0.08); transition: all 0.3s ease;" onmouseover="this.style.boxShadow='0 12px 35px rgba(249, 115, 22, 0.15)'; this.style.transform='translateY(-5px)';" onmouseout="this.style.boxShadow='0 4px 15px rgba(249, 115, 22, 0.08)'; this.style.transform='translateY(0)';">
                <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem;">
                    <div style="width: 50px; height: 50px; border-radius: 0.75rem; background: linear-gradient(135deg, rgba(249, 115, 22, 0.15) 0%, rgba(249, 115, 22, 0.08) 100%); display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-hourglass-half" style="font-size: 1.5rem; color: #f97316;"></i>
                    </div>
                    <div>
                        <p style="color: #64748b; font-size: 0.8rem; margin: 0; font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Pending</p>
                    </div>
                </div>
                <h3 style="color: #f97316; margin: 0; font-weight: 800; font-size: 2rem;">
                    @php
                        $pending = $reports->getCollection()->filter(fn($r) => $r->status === 'pending')->count();
                    @endphp
                    {{ $pending }}
                </h3>
                <p style="color: #64748b; margin: 0.5rem 0 0 0; font-size: 0.85rem;">Awaiting review</p>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-4">
            <div style="background: linear-gradient(135deg, #ffffff 0%, #f0f7ff 100%); border: 1px solid rgba(59, 130, 246, 0.15); border-top: 4px solid #3b82f6; border-radius: 1.2rem; padding: 1.5rem; box-shadow: 0 4px 15px rgba(59, 130, 246, 0.08); transition: all 0.3s ease;" onmouseover="this.style.boxShadow='0 12px 35px rgba(59, 130, 246, 0.15)'; this.style.transform='translateY(-5px)';" onmouseout="this.style.boxShadow='0 4px 15px rgba(59, 130, 246, 0.08)'; this.style.transform='translateY(0)';">
                <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem;">
                    <div style="width: 50px; height: 50px; border-radius: 0.75rem; background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(59, 130, 246, 0.08) 100%); display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-magnifying-glass" style="font-size: 1.5rem; color: #3b82f6;"></i>
                    </div>
                    <div>
                        <p style="color: #64748b; font-size: 0.8rem; margin: 0; font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Under Review</p>
                    </div>
                </div>
                <h3 style="color: #3b82f6; margin: 0; font-weight: 800; font-size: 2rem;">
                    @php
                        $review = $reports->getCollection()->filter(fn($r) => $r->status === 'under_review')->count();
                    @endphp
                    {{ $review }}
                </h3>
                <p style="color: #64748b; margin: 0.5rem 0 0 0; font-size: 0.85rem;">Currently being processed</p>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-4">
            <div style="background: linear-gradient(135deg, #ffffff 0%, #f0f7ff 100%); border: 1px solid rgba(34, 197, 94, 0.15); border-top: 4px solid #22c55e; border-radius: 1.2rem; padding: 1.5rem; box-shadow: 0 4px 15px rgba(34, 197, 94, 0.08); transition: all 0.3s ease;" onmouseover="this.style.boxShadow='0 12px 35px rgba(34, 197, 94, 0.15)'; this.style.transform='translateY(-5px)';" onmouseout="this.style.boxShadow='0 4px 15px rgba(34, 197, 94, 0.08)'; this.style.transform='translateY(0)';">
                <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem;">
                    <div style="width: 50px; height: 50px; border-radius: 0.75rem; background: linear-gradient(135deg, rgba(34, 197, 94, 0.15) 0%, rgba(34, 197, 94, 0.08) 100%); display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-check-circle" style="font-size: 1.5rem; color: #22c55e;"></i>
                    </div>
                    <div>
                        <p style="color: #64748b; font-size: 0.8rem; margin: 0; font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Resolved</p>
                    </div>
                </div>
                <h3 style="color: #22c55e; margin: 0; font-weight: 800; font-size: 2rem;">
                    @php
                        $resolved = $reports->getCollection()->filter(fn($r) => $r->status === 'resolved')->count();
                    @endphp
                    {{ $resolved }}
                </h3>
                <p style="color: #64748b; margin: 0.5rem 0 0 0; font-size: 0.85rem;">Completed actions</p>
            </div>
        </div>
    </div>

    <!-- Filters -->
    <div class="row mb-4">
        <div class="col-12">
            <div style="background: linear-gradient(135deg, #ffffff 0%, #f0f7ff 100%); border: 1px solid rgba(13, 148, 136, 0.15); padding: 2rem; border-radius: 1.2rem; box-shadow: 0 4px 15px rgba(13, 148, 136, 0.08);">
                <h6 style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 1.5rem; font-size: 0.9rem;">
                    <i class="fas fa-filter me-2" style="color: #0d9488;"></i>Filter Reports
                </h6>
                <form method="GET" action="{{ route('admin.reports.index') }}" class="row g-3">
                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-toggle-on me-1" style="color: #0d9488;"></i>Status
                        </label>
                        <select name="status" class="form-select" style="background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); color: #1e293b; border: 1px solid rgba(13, 148, 136, 0.2); padding: 0.75rem 1rem; border-radius: 0.6rem; font-weight: 500; transition: all 0.3s ease;">
                            <option value="">All Statuses</option>
                            <option value="pending" {{ request('status') == 'pending' ? 'selected' : '' }}>Pending</option>
                            <option value="under_review" {{ request('status') == 'under_review' ? 'selected' : '' }}>Under Review</option>
                            <option value="resolved" {{ request('status') == 'resolved' ? 'selected' : '' }}>Resolved</option>
                            <option value="dismissed" {{ request('status') == 'dismissed' ? 'selected' : '' }}>Dismissed</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-exclamation-circle me-1" style="color: #0d9488;"></i>Reason
                        </label>
                        <select name="reason" class="form-select" style="background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); color: #1e293b; border: 1px solid rgba(13, 148, 136, 0.2); padding: 0.75rem 1rem; border-radius: 0.6rem; font-weight: 500; transition: all 0.3s ease;">
                            <option value="">All Reasons</option>
                            <option value="scam_fraud" {{ request('reason') == 'scam_fraud' ? 'selected' : '' }}>Scam/Fraud</option>
                            <option value="inappropriate_content" {{ request('reason') == 'inappropriate_content' ? 'selected' : '' }}>Inappropriate Content</option>
                            <option value="harassment_abuse" {{ request('reason') == 'harassment_abuse' ? 'selected' : '' }}>Harassment/Abuse</option>
                            <option value="fake_listing" {{ request('reason') == 'fake_listing' ? 'selected' : '' }}>Fake Listing</option>
                            <option value="seller_unresponsive" {{ request('reason') == 'seller_unresponsive' ? 'selected' : '' }}>Seller Unresponsive</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-list me-1" style="color: #0d9488;"></i>Type
                        </label>
                        <select name="type" class="form-select" style="background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); color: #1e293b; border: 1px solid rgba(13, 148, 136, 0.2); padding: 0.75rem 1rem; border-radius: 0.6rem; font-weight: 500; transition: all 0.3s ease;">
                            <option value="">All Types</option>
                            <option value="Listing" {{ request('type') == 'Listing' ? 'selected' : '' }}>Listings</option>
                            <option value="Offer" {{ request('type') == 'Offer' ? 'selected' : '' }}>Offers</option>
                            <option value="User" {{ request('type') == 'User' ? 'selected' : '' }}>Users</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label style="color: #1e293b; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-size: 0.8rem; display: block; margin-bottom: 0.75rem;">
                            <i class="fas fa-search me-1" style="color: #0d9488;"></i>Search
                        </label>
                        <input type="text" name="search" placeholder="Search report details..." value="{{ request('search') }}" class="form-control" style="background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); color: #1e293b; border: 1px solid rgba(13, 148, 136, 0.2); padding: 0.75rem 1rem; border-radius: 0.6rem; font-weight: 500; transition: all 0.3s ease;">
                    </div>

                    <div class="col-12">
                        <button type="submit" style="background: linear-gradient(135deg, #0d9488 0%, #06b6d4 100%); color: #ffffff; border: none; padding: 0.75rem 1.5rem; font-weight: 700; border-radius: 0.6rem; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(13, 148, 136, 0.25); cursor: pointer;" onmouseover="this.style.boxShadow='0 6px 18px rgba(13, 148, 136, 0.4)'; this.style.transform='translateY(-2px)';" onmouseout="this.style.boxShadow='0 4px 12px rgba(13, 148, 136, 0.25)'; this.style.transform='translateY(0)';">
                            <i class="fas fa-search me-2"></i>Apply Filters
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Reports Table -->
    <div class="row">
        <div class="col-12">
            <div style="background: linear-gradient(135deg, #ffffff 0%, #f0f7ff 100%); border: 1px solid rgba(13, 148, 136, 0.15); border-radius: 1.2rem; overflow: hidden; box-shadow: 0 4px 15px rgba(13, 148, 136, 0.08);">
                <div style="padding: 1.75rem; border-bottom: 2px solid rgba(13, 148, 136, 0.1); background: linear-gradient(135deg, rgba(13, 148, 136, 0.05) 0%, rgba(6, 182, 212, 0.03) 100%);">
                    <h5 style="color: #1e293b; font-weight: 800; margin: 0; font-size: 1.1rem;">
                        <i class="fas fa-list me-2" style="color: #0d9488;"></i>All Reports
                    </h5>
                </div>

                <div class="table-responsive" style="padding: 0;">
                    @if($reports->count() > 0)
                        <table class="table" style="color: #1e293b; margin-bottom: 0;">
                            <thead style="background: linear-gradient(135deg, rgba(13, 148, 136, 0.08) 0%, rgba(6, 182, 212, 0.05) 100%); border-bottom: 2px solid rgba(13, 148, 136, 0.15);">
                                <tr>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">ID</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Reporter</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Item</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Reason</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Status</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Submitted</th>
                                    <th style="color: #0d9488; font-weight: 800; text-transform: uppercase; font-size: 0.75rem; padding: 1.25rem; letter-spacing: 1px;">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach($reports as $report)
                                    <tr style="border-bottom: 1px solid rgba(13, 148, 136, 0.1); transition: all 0.3s ease;" onmouseover="this.style.backgroundColor='rgba(13, 148, 136, 0.05)';" onmouseout="this.style.backgroundColor='transparent';">
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            <code style="color: #0d9488; font-weight: 700; background: rgba(13, 148, 136, 0.08); padding: 0.3rem 0.6rem; border-radius: 0.4rem;">#{{ $report->id }}</code>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            <div>
                                                <p style="margin: 0; color: #1e293b; font-weight: 600; font-size: 0.95rem;">
                                                    {{ $report->reporter->name }}
                                                </p>
                                                <small style="color: #64748b; font-size: 0.8rem;">{{ $report->reporter->email }}</small>
                                            </div>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            <small style="color: #64748b;">
                                                @php
                                                    $type = class_basename($report->reportable_type);
                                                @endphp
                                                <strong style="color: #1e293b;">{{ $type }}</strong><br>
                                                @if($type === 'Listing')
                                                    <span style="color: #0d9488;">{{ $report->reportable->category ?? 'N/A' }}</span>
                                                @elseif($type === 'Offer')
                                                    Offer #{{ $report->reportable->id }}
                                                @else
                                                    {{ $report->reportable->name ?? 'User' }}
                                                @endif
                                            </small>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            <small style="background: linear-gradient(135deg, rgba(239, 68, 68, 0.15) 0%, rgba(239, 68, 68, 0.08) 100%); color: #ef4444; padding: 0.5rem 0.9rem; border-radius: 0.6rem; font-weight: 700; display: inline-block; border: 1px solid rgba(239, 68, 68, 0.2);">
                                                {{ str_replace('_', ' ', ucfirst($report->reason)) }}
                                            </small>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            @php
                                                $statusColor = match($report->status) {
                                                    'pending' => '#f97316',
                                                    'under_review' => '#3b82f6',
                                                    'resolved' => '#22c55e',
                                                    'dismissed' => '#6b7280',
                                                    default => '#95a5a6'
                                                };
                                                $statusBg = match($report->status) {
                                                    'pending' => 'rgba(249, 115, 22, 0.15)',
                                                    'under_review' => 'rgba(59, 130, 246, 0.15)',
                                                    'resolved' => 'rgba(34, 197, 94, 0.15)',
                                                    'dismissed' => 'rgba(107, 114, 128, 0.15)',
                                                    default => 'rgba(149, 165, 166, 0.15)'
                                                };
                                                $statusText = match($report->status) {
                                                    'pending' => 'Pending',
                                                    'under_review' => 'Under Review',
                                                    'resolved' => 'Resolved',
                                                    'dismissed' => 'Dismissed',
                                                    default => 'Unknown'
                                                };
                                            @endphp
                                            <small style="background: {{ $statusBg }}; color: {{ $statusColor }}; padding: 0.5rem 0.9rem; border-radius: 0.6rem; font-weight: 700; display: inline-block; border: 1px solid {{ $statusColor }}33;">
                                                {{ $statusText }}
                                            </small>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle; font-size: 0.85rem;">
                                            <small style="color: #64748b;">
                                                {{ $report->created_at->diffForHumans() }}
                                            </small>
                                        </td>
                                        <td style="padding: 1.25rem; vertical-align: middle;">
                                            <a href="{{ route('admin.reports.show', $report) }}" style="background: linear-gradient(135deg, #0d9488 0%, #06b6d4 100%); color: white; border: none; font-weight: 700; padding: 0.5rem 1rem; border-radius: 0.6rem; font-size: 0.8rem; text-decoration: none; display: inline-block; transition: all 0.3s ease; box-shadow: 0 2px 8px rgba(13, 148, 136, 0.2);" onmouseover="this.style.boxShadow='0 4px 12px rgba(13, 148, 136, 0.35)'; this.style.transform='translateY(-2px)';" onmouseout="this.style.boxShadow='0 2px 8px rgba(13, 148, 136, 0.2)'; this.style.transform='translateY(0)';">
                                                <i class="fas fa-eye me-1"></i>Review
                                            </a>
                                        </td>
                                    </tr>
                                @endforeach
                            </tbody>
                        </table>

                        <!-- Pagination -->
                        @if($reports->hasPages())
                            <div style="display: flex; justify-content: center; padding: 2rem; border-top: 1px solid rgba(13, 148, 136, 0.1); background: rgba(13, 148, 136, 0.03);">
                                {{ $reports->links() }}
                            </div>
                        @endif
                    @else
                        <!-- Empty State -->
                        <div style="text-align: center; padding: 4rem 2rem;">
                            <div style="background: linear-gradient(135deg, rgba(13, 148, 136, 0.1), rgba(6, 182, 212, 0.1)); width: 120px; height: 120px; border-radius: 50%; margin: 0 auto 2rem; display: flex; align-items: center; justify-content: center;">
                                <i class="fas fa-inbox" style="font-size: 3.5rem; color: rgba(13, 148, 136, 0.3);"></i>
                            </div>
                            <h5 style="color: #1e293b; font-weight: 700; font-size: 1.2rem; margin-bottom: 0.5rem;">No reports found</h5>
                            <p style="color: #64748b; margin: 0;">There are no reports matching your filters. Your users are behaving well!</p>
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>
</div>

@endsection
